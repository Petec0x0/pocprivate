# poc-private (PRIVATE - do not publish)

Local vault for security PoCs moved out of the public `Petec0x0/Petec0x0` profile repo.

Moved on 2026-09-20:
- lean-camera-test.html
- lean-camera-test-fast.html
- poc-bridge-probe.html

These were previously live at `https://petec0x0.github.io/Petec0x0/*.html` via GitHub Pages.
Do NOT push this repo to a public remote. When you create the GitHub repo, create it as **Private**.

Intended remote (create it first, then run):
```
git remote add origin https://github.com/<your-user>/poc-private.git
git push -u origin main
```
